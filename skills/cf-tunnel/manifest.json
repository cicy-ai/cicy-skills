{
  "$schema": "https://skills.cicy-ai.com/v1/manifest.schema.json",
  "name": "cf-tunnel",
  "version": "1.1.3",
  "title": "Cloudflare Tunnel",
  "description": "Manage Cloudflare NAMED tunnels (token/name → hostname list), routes and DNS. Subcommands: config / status / tunnels / sync / create / rm / list / add / del.",
  "category": "cloud",
  "tags": [
    "cloudflare",
    "tunnel",
    "dns"
  ],
  "author": "cicy-ai",
  "homepage": "https://github.com/cicy-ai/cicy-skills/tree/main/skills/cf-tunnel",
  "license": "MIT",
  "runtime": {
    "node": ">=18"
  },
  "system_requirements": [],
  "npm_dependencies": false,
  "entry": "bin/cf-tunnel",
  "config": {
    "path": "~/cicy-ai/db/cf-tunnel.json",
    "permissions": "0600",
    "secret_fields": [
      "accounts.*.api_token",
      "accounts.*.account_id",
      "accounts.*.zone_id",
      "accounts.*.tunnels.*.token"
    ]
  },
  "permissions": [
    "network",
    "filesystem:db"
  ],
  "compatible_agents": [
    "*"
  ],
  "files": {
    "skill_md": "SKILL.md",
    "help_md": "references/help.md",
    "tools_md": "references/tools.md",
    "readme": "README.md",
    "help_en_md": "references/help.en.md",
    "help_cn_md": "references/help.cn.md",
    "tools_en_md": "references/tools.en.md",
    "tools_cn_md": "references/tools.cn.md"
  },
  "i18n": {
    "zh-CN": {
      "title": "Cloudflare Tunnel",
      "description": "管理 Cloudflare 具名隧道(token/name → 域名列表)、路由和 DNS。子命令:config / status / tunnels / sync / create / rm / list / add / del。"
    }
  },
  "tools": [
    {
      "name": "cf-tunnel tunnels",
      "example": "cf-tunnel tunnels",
      "description": "Tree of named tunnels → their hostnames (live from CF API)"
    },
    {
      "name": "cf-tunnel sync",
      "example": "cf-tunnel sync",
      "description": "Regenerate accounts.<key>.tunnels {id, token, hostnames[]} in ~/cicy-ai/db/cf-tunnel.json from the CF API"
    },
    {
      "name": "cf-tunnel create",
      "example": "cf-tunnel create cloudshell --port 8008",
      "description": "Provision a named tunnel end-to-end (tunnel + ingress + DNS + connector token → registry)"
    },
    {
      "name": "cf-tunnel rm",
      "example": "cf-tunnel rm cloudshell",
      "description": "Delete a named tunnel, its DNS records and its registry entry"
    },
    {
      "name": "cf-tunnel config",
      "example": "cf-tunnel config",
      "description": "Show/edit credential configuration"
    },
    {
      "name": "cf-tunnel status",
      "example": "cf-tunnel status",
      "description": "Check config state and registered tunnel names"
    },
    {
      "name": "cf-tunnel list",
      "example": "cf-tunnel list",
      "description": "(legacy) List all routes of the fixed tunnel_id"
    },
    {
      "name": "cf-tunnel add",
      "example": "cf-tunnel add 8080",
      "description": "(legacy) Add <port>.<domain> route with DNS record"
    },
    {
      "name": "cf-tunnel del",
      "example": "cf-tunnel del 8080",
      "description": "(legacy) Remove a <port>.<domain> route and its DNS record (also matches old g-<port>)"
    }
  ]
}
